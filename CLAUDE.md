# Agent-Ready Benchmark

**开始任何工作前，先读 [`PROJECT-LOG.md`](PROJECT-LOG.md)** —— 它是本项目的工作记忆：当前状态、从哪继续、关键决策与 why、会话历史。

评分标准在 `scoring-model-design.md`（当前 v0.3）。

收尾约定：每次 session 结束时更新 PROJECT-LOG.md 的「当前状态」，并在「会话日志」追加一条。

工作流：直接推 main，无分支无 PR（仅在被要求时提交）。
